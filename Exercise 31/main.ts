//Task 31

let username: string[]  = [];  //empty array for users

//check if the list of users is not empty
if
(username.length === 0){
    console.log("We need to find some users!");
}else {
    //Remove all users from the array
    username = [];
    console.log("All usernames have been removed.")
}